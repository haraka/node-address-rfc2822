engines:
  eslint:
    enabled: true
    channel: 'eslint-9'
    config:
      config: 'eslint.config.mjs'

ratings:
  paths:
    - '**.js'

checks:
  method-complexity:
    config:
      threshold: 10
